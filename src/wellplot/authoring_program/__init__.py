"""Provider-independent boundary for restricted Wellplot authoring programs.

CM-01 establishes this package as an inert architecture marker. Later slices
may add the parser, validator, interpreter, and SDK only when their dependency
rules remain compatible with the Code Mode architecture contract.
"""
